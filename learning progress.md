# Learning Progress

## Day 1 - JavaScript DOM Basics

Objective: Learn how JavaScript can create, fill, place, remove, and change HTML elements on a live page.

Practice files:
- `learning day 1/index.html`
- `learning day 1/style.css`
- `learning day 1/script.js`

Things to learn:
- Create a new element with `document.createElement()`.
- Fill an element with data using `textContent`.
- Put an element inside a `div` on the live page using `appendChild()`.
- Remove an element using `.remove()`.
- Change an attribute using `setAttribute()`.

Definition of done:
- [x] Create one new tag item.
- [x] Fill that tag with text or data.
- [x] Put that tag inside the practice `div`.
- [x] Remove one element from the page.
- [x] Change one element attribute.

My notes:
Day 1 completed. I created a paragraph with JavaScript, added text to it, placed it inside the practice div, gave it a class attribute, and removed it with a button click.

## Day 2 - Events, Text Changes, and Class Control

Objective: Learn how JavaScript responds to user actions and changes the page without creating everything from scratch.

Practice files:
- `learning day 1/index.html`
- `learning day 1/style.css`
- `learning day 1/script.js`

Things to learn:
- Use `addEventListener("click", function)` instead of only `.onclick`.
- Change existing text using `.textContent`.
- Add, remove, and toggle CSS classes using `classList`.
- Read and update a number stored in a variable.
- Disable or enable a button using `.disabled`.

Definition of done:
- [ ] Create a counter number that starts at `0`.
- [ ] Add an Increase button that adds `1` to the counter.
- [ ] Add a Decrease button that subtracts `1` from the counter.
- [ ] Add a Reset button that changes the counter back to `0`.
- [ ] Show the counter value on the page using `.textContent`.
- [ ] Use `addEventListener()` for all button clicks.
- [ ] Use `classList.add()` when the counter is positive.
- [ ] Use `classList.add()` when the counter is negative.
- [ ] Use `classList.remove()` or `classList.toggle()` when needed.
- [ ] Disable the Decrease button when the counter is `0`, then enable it when the counter becomes greater than `0`.

Exercises:
- Exercise 1: Select the counter display and the three buttons.
- Exercise 2: Create a variable named `count` and set it to `0`.
- Exercise 3: Write a function named `updateCounter()` that displays the current count.
- Exercise 4: Make the Increase button update the count and refresh the page text.
- Exercise 5: Make the Decrease button update the count and refresh the page text.
- Exercise 6: Make the Reset button set the count back to `0`.
- Exercise 7: Add CSS classes for positive, negative, and neutral counter states.
- Exercise 8: Disable the Decrease button when count is `0`.

My notes:
Not started yet.
